/**
 * Captured before the bundled Alpine module body runs, so the host page's own Alpine
 * reference survives if the npm build ever starts assigning window.Alpine.
 *
 * @type {unknown}
 */
export const hostAlpine = window.Alpine
